<?php
/**
 * Created by PhpStorm.
 * User: ndepaz
 * Date: 12/6/2018
 * Time: 9:59 PM
 */

namespace  {
    exit("This file should not be included, only analyzed by your IDE");
}

namespace Illuminate\Support{
    /**
     * @method Collection select($properties,callback $callback) {
     *     Extends laravel collection class adding the ability to select properties as if these were SQL attributes.
     *     It is helpful when you are ready to send an object but not all of it's properties to the front end,
     *     and would like to expose only what is necessary for front-end to receive.
     *
     *     @param array|string $properties select statements: "id", "id as identified", "user.profile.first_name as name".
     *     @param callable $lastArgument the last argument can be a function callback which passes two objects—$dummy
     *      by reference and $object by value. Used to set additional properties.
     *
     *     @return Collection
     * }
     */
    class Collection
    {

    }
}
namespace Illuminate\Database\Eloquent{
    /**
     * @method Collection select($properties,callback $callback) {
     *     Extends laravel collection class adding the ability to select properties as if these were SQL attributes.
     *     It is helpful when you are ready to send an object but not all of it's properties to the front end,
     *     and would like to expose only what is necessary for front-end to receive.
     *
     *     @param array|string $properties select statements: "id", "id as identified", "user.profile.first_name as name".
     *     @param callable $lastArgument the last argument can be a function callback which passes two objects—$dummy
     *      by reference and $object by value. Used to set additional properties.
     *
     *     @return Collection
     * }
     */
    class Collection
    {

    }

}